
# Lesson 2.5: Writing Dual PRD (Human + Agent) Using Context

---

## Overview

In previous lessons, you completed market research and user research. Now you'll write **dual PRDs** - one for humans (stakeholders) and one for agents (automation systems).

**Why Dual PRDs?**
- **Human PRD (Markdown):** For PMs, engineers, designers, executives - readable documentation
- **Agent PRD (JSON):** For automation - ticket writers, test generators, deployment agents - machine-readable specifications

**What You'll Build:**
- Comprehensive human-readable PRD for Lease Compliance Reporting feature
- Agent-consumable JSON PRD with task graphs, handoff contracts, and technical schemas

**What You'll Learn (8 Claude Code Skills):**
While working through this lab, you'll use advanced Claude Code techniques including context chaining, cross-referencing, iterative refinement, template customization, prompt engineering, output validation, and more. These skills are embedded in the prompts you'll use.

**Time Required:** 30-40 minutes

---

## Prerequisites

Before starting this lesson, make sure you have:
- Completed Module 1
- Completed all previous lessons of Module 2 (especially 2.3 and 2.4)
- Market research file in `outputs/market-research.md`
- User research file in `outputs/user-research.md`
- Enhanced PRD template in `templates/prd-template-redesigned.md`
- Agent PRD schema in `templates/prd-schema.json`
- CLAUDE.md configured with appropriate context

---

## Hands-On: Writing Dual PRD

### Step 1: Open Your Project

Open your VS Code and navigate to your project that you cloned in Module 1.

1. Click on the **Open** button in VS Code
2. Select the folder you cloned in Module 1

![images](./images/m1.png)

---

### Step 2: Open Terminal and Launch Claude

1. Click on **Terminal** in the menu bar → **New Terminal**
2. Type `claude` and press **Enter**

![images](./images/m2.png)
![images](./images/m3.png)

---

## PART A: Generate Human-Readable PRD

### Step 3: Generate Comprehensive Human PRD

Now let's write a comprehensive human PRD using context chaining and specific prompting:

**Prompt:**
```
Write comprehensive PRD for Lease Compliance Reporting feature.

Context sources to use:
- @outputs/market-research.md for market opportunity, trends, competitive analysis
- @outputs/user-research.md for user personas, pain points, interview insights
- @company-context/ for company strategy, positioning, OKRs
- @templates/prd-template-redesigned.md for structure

Follow the template structure BUT customize with:
- Custom section: "Regulatory Compliance Metrics" (audit accuracy rate, compliance reporting time reduction, supported accounting standards like IFRS and GAAP)

Required sections:
1. Problem statement with market sizing
2. User personas (Legal Team Lead, Compliance Officer, CFO)
3. User stories with acceptance criteria and priority (P0/P1/P2)
4. Feature requirements (functional + non-functional)
5. End-to-end user journey: Jennifer (Legal Team Lead, 150 leases, audit in 10 days) - step-by-step from upload to auditor delivery
6. Non-functional requirements table: performance, security, data retention, availability
7. Technical specifications: data models (Lease, ComplianceReport, AuditTrail), API specs, integration points (QuickBooks, NetSuite, SAP)
8. Success metrics and guardrail metrics
9. Milestones and GTM plan

Level of detail: Comprehensive with specific numbers, timelines, and technical depth.

Save to: outputs/prd-lease-compliance.md
```

**💡 What's Happening Here?**

This prompt demonstrates 3 key Claude Code skills:
1. **Context Chaining:** Explicitly referencing multiple context layers (@outputs, @templates, @company-context)
2. **Prompt Engineering:** Specific requirements (what, how detailed) vs. vague "write a PRD"
3. **Template Customization:** Following template structure while adding domain-specific sections

Claude reads all your previous research and company context to generate a comprehensive PRD.

**Expected Output:** Complete human-readable PRD (~3000-4000 words) saved to outputs folder.

---

### Step 4: Add Citations and Cross-References

Now let's make the PRD traceable by adding citations to research findings:

**Prompt:**
```
Review the PRD you just created (outputs/prd-lease-compliance.md).

Add citations for all claims, data points, and user insights:

1. For market statistics: Add inline citations [Source: market-research.md:line_number]
2. For user pain points: Add citations [Source: user-research.md:line_number]
3. For user quotes: Include direct quotes from user interviews if available

Example citation format:
"Real estate legal teams manage an average of 500+ leases [Source: market-research.md:45], spending 40 hours/month on manual compliance reporting [Source: user-research.md:78]."

Ensure every key claim is traceable to source research.

Update outputs/prd-lease-compliance.md with citations.
```

**💡 What's Happening Here?**

This demonstrates **Cross-Referencing & Citation Management:**
- Creating evidence-based, traceable documents
- Stakeholders can verify claims by checking source line numbers
- Critical for enterprise PM work and gaining stakeholder trust

**Expected Output:** PRD with inline citations linking every claim to specific research findings.

---

### Step 5: Refine Specific Section (Iterative Editing)

Let's improve the Technical Specifications section without rewriting the entire PRD:

**Prompt:**
```
I want to expand the Technical Specifications section only.

For the Technical Specifications section in outputs/prd-lease-compliance.md:

Expand the Integration Points subsection to include:
- Specific accounting systems: QuickBooks (API v3), NetSuite (SuiteTalk REST), SAP (OData), Workday (REST API)
- Authentication methods: OAuth 2.0 for each system
- Data sync frequency options: Real-time (webhook), Hourly, Daily, Manual
- Error handling: Retry logic (3 attempts with exponential backoff), fallback to manual entry
- Rate limits: QuickBooks (500 req/min), NetSuite (1000 req/hour), SAP (custom), Workday (200 req/min)

Edit ONLY the Integration Points subsection - leave all other sections unchanged.
```

**💡 What's Happening Here?**

This demonstrates **Iterative Refinement & Section-Level Editing:**
- Refining specific sections without regenerating entire document
- Critical skill for production work - you rarely rewrite everything
- Saves time and preserves other sections that are already good

**Expected Output:** Enhanced Integration Points subsection with detailed technical specs.

---

### Step 6: Validate Human PRD

Before moving to agent PRD, let's validate the human PRD for completeness:

**Prompt:**
```
Validate outputs/prd-lease-compliance.md for quality and completeness.

Check against these criteria:

1. Template Compliance:
   - Compare against @templates/prd-template-redesigned.md
   - List any missing sections
   - Verify all required sections are present

2. Citation Quality:
   - Count total citations
   - Identify any unsupported claims that need citations
   - Check citation format consistency

3. Cross-Reference Coherence:
   - Do user personas align with user research?
   - Do feature requirements address the problem statement?
   - Do success metrics map to business goals?

4. Completeness:
   - Are all user pain points from user research addressed?
   - Are all market opportunities captured?
   - Are technical specifications complete?
   - Are NFRs (non-functional requirements) fully defined?

Provide a validation report with specific issues found and recommendations.
```

**💡 What's Happening Here?**

This demonstrates **Output Validation & Quality Checks:**
- Having Claude self-review outputs against requirements
- Catches gaps, inconsistencies, and missing elements before stakeholder review
- One of Claude's superpowers - systematic validation

**Expected Output:** Validation report identifying any gaps or issues.

**Fix Issues (if any):**
```
Based on your validation report, fix all identified issues in outputs/prd-lease-compliance.md.
```

---

## PART B: Generate Agent-Consumable PRD

### Step 7: Generate Agent PRD (JSON)

Now let's create the machine-readable version for automation systems:

**Prompt:**
```
Generate an agent-consumable PRD in JSON format based on the human PRD (outputs/prd-lease-compliance.md).

Follow the schema structure in @templates/prd-schema.json.

Include ALL sections from human PRD in structured format:

1. **metadata:** version, generated_date, human_prd_reference

2. **introduction:** problem_statement, key_insights, impact_sizing (structured objects)

3. **goals_and_metrics:** success_metrics[], guardrail_metrics[]

4. **user_stories[]:** Extract all user stories with structured fields:
   - persona, user_story, acceptance_criteria[], priority (P0/P1/P2), complexity (Low/Medium/High)

5. **nfrs (non-functional requirements):** Structure as typed objects:
   - performance: {response_time_ms, throughput, max_concurrent_users}
   - security: {authentication, encryption_at_rest, encryption_in_transit, compliance_standards[]}
   - availability: {uptime_sla_percent, rto_hours, rpo_hours, backup_frequency}
   - data_retention: {retention_period_years, deletion_policy, pii_handling}

6. **technical_specs:**
   - data_models: {Lease: {fields, types, relationships}, ComplianceReport: {...}, AuditTrail: {...}}
   - api_specs: {endpoints[], auth_method, rate_limits{}}
   - integrations: {systems[], sync_frequency, webhook_support}

7. **agent_workflow:**

   a) task_graph (DAG - Directed Acyclic Graph):
   ```
   Nodes:
   - lease_data_extraction (Agent: DataIngestionAgent, action: "Extract lease data from PDF/CSV")
   - data_validation (Agent: ValidationAgent, action: "Validate data completeness and format", depends_on: [lease_data_extraction])
   - compliance_check (Agent: ComplianceAgent, action: "Check IFRS/GAAP compliance", depends_on: [data_validation])
   - risk_scoring (Agent: RiskScoringAgent, action: "Calculate risk scores", depends_on: [data_validation])
   - report_generation (Agent: ReportAgent, action: "Generate audit report", depends_on: [compliance_check, risk_scoring])
   ```

   b) handoff_contracts[] - For each agent handoff, define:
   ```
   {
     "from_agent": "DataIngestionAgent",
     "to_agent": "ValidationAgent",
     "input_schema": {
       "lease_data": {"type": "array", "required_fields": ["lease_id", "tenant", "start_date", "end_date", "rent"]},
       "source_file": {"type": "string"}
     },
     "output_schema": {
       "validated_leases": {"type": "array"},
       "validation_errors": {"type": "array"}
     },
     "invariants": ["All lease_ids unique", "Dates in ISO8601"],
     "failure_modes": [
       {"condition": "Malformed input", "action": "Return error, halt pipeline"},
       {"condition": "Partial data", "action": "Log warning, process valid records"}
     ]
   }
   ```

   c) tool_manifest[] - List all tools:
   ```
   {
     "tool_id": "pdf_lease_extractor",
     "tool_name": "PDF Lease Data Extractor",
     "owning_agent": "DataIngestionAgent",
     "type": "API",
     "idempotent": true,
     "timeout_ms": 30000,
     "retry_strategy": "exponential_backoff",
     "fallback": "Manual upload form"
   }
   ```

8. **milestones:** Extract from human PRD, structure as objects with dates

9. **validation:** go_no_go_criteria[] as boolean checks
   - Example: {"criterion": "All P0 features complete", "must_pass": true}

Format: Pretty-printed JSON with 2-space indentation

Save to: outputs/prd-lease-compliance-agent.json
```

**💡 What's Happening Here?**

This creates a machine-readable PRD that:
- **Mirrors all human PRD content** in structured format
- **Adds agent workflow specifications** (task graph, handoff contracts, tool manifest)
- **Enables automation** - downstream agents can parse JSON directly without interpreting prose

**Key Concepts:**
- **Task Graph (DAG):** Shows which agent tasks can run in parallel vs. sequentially
- **Handoff Contracts:** Explicit schemas for agent-to-agent data transfer (prevents silent failures)
- **Tool Manifest:** Lists all tools with idempotency flags (critical for retry behavior)

**Expected Output:** Complete agent PRD JSON file (~500-800 lines).

---

### Step 8: Understanding Task Graphs (DAG)

Let's visualize the task graph to understand parallel execution:

**Prompt:**
```
Create a visual representation of the task graph from outputs/prd-lease-compliance-agent.json.

Generate a simple text-based flowchart showing:
- All agent nodes
- Dependencies (arrows)
- Which tasks can run in parallel

Save as: outputs/task-graph-visualization.md
```

**💡 What's Happening Here?**

**Task Graph Basics:**

Linear execution (sequential):
```
Extract → Validate → Compliance → Risk → Report
(Total time: Sum of all steps)
```

Task Graph (DAG with parallelization):
```
Extract Lease Data (DataIngestionAgent)
         ↓
Validate Data (ValidationAgent)
         ↓
    ┌────┴────┐
    ↓         ↓
Compliance  Risk Scoring
  Check    (can run in parallel!)
    ↓         ↓
    └────┬────┘
         ↓
  Generate Report
```

**Benefit:** Compliance Check and Risk Scoring can run simultaneously, reducing total execution time.

**Expected Output:** Visual task graph showing parallel execution opportunities.

---

### Step 9: Validate Agent PRD

Let's validate the agent PRD for completeness and alignment with human PRD:

**Prompt:**
```
Validate outputs/prd-lease-compliance-agent.json for quality and alignment.

Check:

1. Schema Compliance:
   - Does it follow @templates/prd-schema.json structure?
   - Are all required fields present?
   - Are types correct?

2. Task Graph Validation:
   - Are there any circular dependencies? (should be none - must be DAG)
   - Are all dependencies valid (referenced tasks exist)?
   - Are parallel execution opportunities identified?

3. Handoff Contract Completeness:
   - Is input_schema complete for each handoff?
   - Is output_schema complete?
   - Are failure_modes defined?
   - Are invariants clear?

4. Alignment with Human PRD:
   - Do user stories match between human and agent PRDs?
   - Do technical specs align?
   - Are NFRs consistent?

Provide validation report with specific issues (if any).
```

**Expected Output:** Validation report confirming agent PRD quality.

**Fix Issues (if any):**
```
Based on validation report, fix all identified issues in outputs/prd-lease-compliance-agent.json.
```

---

## Optional: Advanced Techniques

### Optional Step 10: Create PRD Version 2 (Version Management)

Want to explore an alternative approach? Create a second version:

**Prompt:**
```
Create a v2 of the PRD that shifts focus from manual compliance reporting to automated compliance monitoring.

Key changes:
- Emphasize real-time monitoring vs. one-time report generation
- Add proactive alerts for lease compliance issues
- Include dashboard for continuous monitoring

Save as: outputs/prd-lease-compliance-v2.md

Then create a comparison document showing key differences between v1 and v2.
Save as: outputs/prd-comparison-v1-v2.md
```

**💡 What's Happening Here?**

This demonstrates **Version Management:**
- Exploring alternative product directions
- Comparing different approaches
- Useful for presenting options to stakeholders

---

### Optional Step 11: Context Priority & Conflict Resolution

Understanding what happens when instructions conflict:

**Prompt:**
```
Write a brief PRD section for "User Authentication" following @templates/prd-template-redesigned.md.

BUT keep it to 200 words maximum (brief).

Note: Template says "comprehensive documentation" but I'm asking for "brief" - how do you handle this conflict?

After generating, explain which instruction took priority and why.
```

**💡 What's Happening Here?**

This demonstrates **Context Priority & Conflict Resolution:**
- Understanding how Claude handles conflicting instructions
- Inline prompts generally take priority over CLAUDE.md context
- But Claude will explain trade-offs

---

### Optional Step 12: Task Decomposition (Brief Example)

For very complex PRDs, you can break work into managed tasks:

**Prompt:**
```
Show me how to use task decomposition for a complex PRD.

Create a task list using TaskCreate for writing a PRD with these phases:
1. Problem definition & market context
2. User personas & stories
3. Feature requirements
4. Success metrics
5. Technical specifications
6. Agent workflow design
7. Validation & review

Don't execute the tasks - just show the task structure.
```

**💡 What's Happening Here?**

This demonstrates **Task Decomposition:**
- Breaking complex documents into manageable chunks
- Useful for very large PRDs or when working with teams
- Each task can be assigned, tracked, and reviewed independently

---

## What You Learned: 8 Claude Code Skills

Congratulations! You've used 8 advanced Claude Code skills:

1. ✅ **Context Chaining (Level 101)** - Used multiple context layers (@outputs, @templates, @company-context) strategically
2. ✅ **Iterative Refinement (Level 101)** - Refined specific sections without rewriting entire document
3. ✅ **Task Decomposition (Level 101)** - Saw how to break complex documents into manageable tasks
4. ✅ **Prompt Engineering (Level 101)** - Controlled detail level with specific vs. broad prompts
5. ✅ **Cross-Referencing (Level 201)** - Added citations with file:line_number for traceability
6. ✅ **Template Customization (Level 201)** - Extended template with domain-specific sections
7. ✅ **Output Validation (Level 201)** - Had Claude self-review for quality and completeness
8. ✅ **Context Priority (Level 201)** - Understood how Claude handles conflicting instructions

**Plus, you learned:**
- Dual PRD generation (human + agent)
- Task graphs and DAG concepts
- Handoff contracts for agent-to-agent communication
- Tool manifests with idempotency
- Non-functional requirements specification

---

## Summary: What You Created

**Files Generated:**
- ✅ `outputs/prd-lease-compliance.md` - Human-readable PRD (~3000-4000 words)
- ✅ `outputs/prd-lease-compliance-agent.json` - Agent-consumable PRD (structured JSON)
- ✅ `outputs/task-graph-visualization.md` - Visual task graph
- ⚠️ Optional: `outputs/prd-lease-compliance-v2.md` - Alternative version
- ⚠️ Optional: `outputs/prd-comparison-v1-v2.md` - Version comparison

**Dual PRD Benefits:**
- **Human PRD:** Stakeholder communication, alignment, documentation
- **Agent PRD:** Automation, ticket generation, test creation, deployment orchestration

**When to Use:**
- **Always create human PRD** - for all features
- **Create agent PRD when:** Building features with automation, multi-agent workflows, or complex integrations

---

## Module 2 Summary

Congratulations! You've completed Module 2. Here's your learning journey:

1. **Lesson 2.1-2.2:** Context system setup with CLAUDE.md
2. **Lesson 2.3:** Market research using Claude Code
3. **Lesson 2.4:** User research with web search
4. **Lesson 2.5:** Dual PRD writing with advanced Claude Code skills

**Skills You Now Have:**
- Context management and chaining
- Research automation and synthesis
- Advanced prompt engineering
- Dual PRD generation (human + agent)
- Task graphs and agentic workflows
- Citation management and validation
- Iterative refinement techniques

**Real-World Applications:**
- Write PRDs faster with better quality
- Create traceable, evidence-based documents
- Build agent-ready specifications
- Manage complex documentation projects
- Enable downstream automation

---

## What's Next: Module 3

In Module 3, you'll explore:
- **Agentic workflows** - Multi-agent systems and orchestration
- **Data visualization** - Creating charts from research
- **Interactive workflows** - Building PM tools
- **Advanced automation** - Recurring PM workflows

---

## Practice Exercises

Reinforce your skills:

**Exercise 1: Different Feature (30 min)**
Write dual PRD for "Automated Lease Renewal Notifications" using all the skills learned.

**Exercise 2: Reverse Engineering (20 min)**
Take an existing PRD and add citations, NFRs, and create agent version.

**Exercise 3: Task Graph Design (15 min)**
For "Contract Risk Scoring" feature, design a task graph with 5-7 agents showing parallel execution.

**Exercise 4: Handoff Contract (20 min)**
Write complete handoff contract for "Data Extractor → Validator" agents with schemas and failure modes.

---

## Additional Resources

- **Claude Code Documentation:** [Context System](https://docs.anthropic.com/claude-code/context)
- **PRD Templates:** Review `templates/prd-template-redesigned.md` and `templates/prd-schema.json`
- **Community:** Share your PRDs in course Discord for peer review

---

**End of Lesson 2.5**
